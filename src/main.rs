use mev_relay::Result;

fn main() -> Result<()> {
    println!("MEV Relay - Pool Filtering System");
    println!("Core functionality is working!");
    println!("Pool filtering tests are available.");
    
    Ok(())
} 